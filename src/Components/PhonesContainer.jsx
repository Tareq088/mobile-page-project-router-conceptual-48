import React from 'react';

const PhonesContainer = ({phones}) => {
    console.log(phones)
    return (
        <div>
            phones Container
        </div>
    );
};

export default PhonesContainer;